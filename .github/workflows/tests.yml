name: docker build and test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out solr
        uses: actions/checkout@v1
        with:
          repository: freelawproject/courtlistener-solr-server
          ref: origin/master
      - name: Check out CourtListener
        uses: actions/checkout@v1
      - name: Create docker network
        run: docker network create -d bridge --attachable cl_net_overlay
      - name: Start docker compose
        working-directory: docker/courtlistener
        run: docker-compose up -d
      - name: List docker statuses
        run: docker ps -a
      - name: Show the logs
        working-directory: docker/courtlistener
        run: docker-compose logs
        
#       - name: Run the tests!
# #         run: docker exec cl-django python /opt/courtlistener/manage.py test cl
#         run: python /opt/courtlistener/manage.py test cl
#         container: cl-django
