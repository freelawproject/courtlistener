# .hgignore file

# Seperated into two sections, glob, and regexp
# Globs are shell-style regular expressions, as distinct from perl-style


# Beginning with the globs
syntax: glob

# no compiled files!
*.class
*.pyc
*~

# no private files!
*[P|p][R|r][I|i][V|v][A|a][T|t][E|e]*

# ignore the django directory
django
