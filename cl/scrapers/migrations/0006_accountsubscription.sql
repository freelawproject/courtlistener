BEGIN;
--
-- Create model AccountSubscription
--
-- PERF: CREATE TABLE is safe. No locks on existing tables. Instantaneous.
-- The inline UNIQUE constraint creates an index, but since this is a new
-- (empty) table, there is no locking concern on existing data.
CREATE TABLE "scrapers_accountsubscription" ("id" integer NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY, "scraper" integer NOT NULL, "email" varchar(254) NOT NULL, "user_name" varchar(30) NOT NULL, "first_subscription" date NOT NULL, "last_subscription" date NOT NULL, CONSTRAINT "unique_scraper_user_name" UNIQUE ("scraper", "user_name"));
COMMIT;
